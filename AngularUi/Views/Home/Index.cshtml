@{
    ViewBag.Title = "Home Page";
}

@*<div ng-app="myApp" ng-controller="EmployeeController" class="table-responsive">
    <table id="mytable" class="table table-bordred table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Edit</th>
                <th>Delete</th>

            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="item in ListOfEmployee">
                <td>{{item.EmployeeID}}</td>
                <td>{{item.FirstName}}</td>
                <td>{{item.LastName}}</td>

                <td>
                    <a class="btn btn-primary btn-xs" href="../../App/Views/EditEmployee.html/{{item.EmployeeID}}">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                </td>
                <td>
                    <a class="btn btn-danger btn-xs" href="../../App/Views/DeleteEmployee.html/{{item.EmployeeID}}">
                        <span class="glyphicon glyphicon-trash"></span>
                    </a>
                </td>
            </tr>
        </tbody>
    </table>

</div>
<script src="~/Scripts/angular.min.js"></script>
<script src="~/Scripts/angular-route.min.js"></script>
<script src="~/App/EmployeeController.js"></script>*@